type Wizard {
    id: ID!,
    type: String,
    name: String,
    powerLevel: Int
}

type Query {
    wizards:[Wizard]
    wizard(id: ID):Wizard
}

type Mutation {
    addWizard(type:String!, name: String!, powerLevel: Int):Wizard
}